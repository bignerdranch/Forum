<%= @forum_topic.title %>
<%= form_for ([@forum_topic, @post]) do |f| %>
  <%= f.label :content %>
  <%= f.text_area :content %><br />

  <%= f.hidden_field :user_id, value: :current_user.id %>

  <%= f.submit "Submit" %>
<% end %>
